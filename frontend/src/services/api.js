// SHARED INTEGRATION FILE — coordinate before editing.
// Base fetch wrapper other services build on. Not implemented yet.
const BASE_URL = import.meta.env.VITE_API_URL

export async function apiFetch(path, options) {
  // TODO: implement
}
